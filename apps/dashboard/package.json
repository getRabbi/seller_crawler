{
  "name": "@seller-intelligence/dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@seller-intelligence/shared-types": "0.0.0"
  },
  "scripts": {
    "dev": "next dev",
    "lint": "eslint app components lib tests next.config.ts",
    "typecheck": "next typegen && tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. apps/dashboard/tests",
    "build": "node scripts/validate-build-env.mjs && next build"
  },
  "devDependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
