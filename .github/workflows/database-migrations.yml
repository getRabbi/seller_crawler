name: Hosted D1 Migration Handoff (Manual)

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hosted migrations are blocked until all eight D1 resources are recorded."
          echo "Use DEPLOYMENT_RUNBOOK.md and migrate core, contacts, operations, then history."
          exit 1
