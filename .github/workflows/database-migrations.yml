name: Database Migrations Disabled

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Database migrations are not implemented in Phase 0."
          echo "Phase 1 owns local D1 schema and migrations."
          exit 1
