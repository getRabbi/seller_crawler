name: Database Migrations Disabled

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hosted database migrations are blocked during local phase reconciliation."
          echo "Phase 1 local D1-compatible schema and migrations are validated locally only."
          exit 1
