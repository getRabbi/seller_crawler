name: Worker Deployment Handoff (Manual)

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Worker deployment is blocked until OPERATOR_INPUTS_REQUIRED.md is complete."
          echo "Use DEPLOYMENT_RUNBOOK.md for the operator-led staging release."
          exit 1
